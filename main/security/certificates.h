#ifndef CERTIFICATES_H
#define CERTIFICATES_H

/*
 * TLS certificate placeholders.  In a real project the certificate
 * data would be stored in a header file or loaded from storage.  For
 * this skeleton we define empty strings.
 */

extern const char server_cert_pem_start[];
extern const char server_cert_pem_end[];

#endif /* CERTIFICATES_H */